{
  "name": "lm-nextra",
  "private": true,
  "workspaces": [
    "packages/nextra",
    "packages/nextra-theme-docs",
    "src"
  ],
  "scripts": {
    "start": "yarn workspace src start",
    "dev": "yarn workspace src dev",
    "export": "yarn workspace src export",
    "build": "yarn build:core && yarn build:theme-docs && yarn workspace src build",
    "build:src": "yarn workspace src build",
    "build:core": "yarn workspace nextra build",
    "build:theme-docs": "yarn workspace nextra-theme-docs build"
  }
}
